/* NAO CONSEGUI USAR
interface PersonType {
    value: string;
    viewValue: string;
}

function createTypes() {
    let personTypes: PersonType[] = [
        {value: 'supplier', viewValue: 'Fornecedor'},
        {value: 'client', viewValue: 'Cliente'},
        {value: 'employee', viewValue: 'Empregado'}
      ];

    return personTypes;
}

var personTypes = createTypes(); */